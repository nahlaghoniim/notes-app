# 📝 Notes App (PHP + MySQL)

A simple and clean Notes Management application built using **PHP**, **MySQL**, and **PDO**.  
This project demonstrates backend fundamentals such as CRUD operations, input sanitization, and a basic MVC-like structure.

---

##  Features

✔ Create new notes  
✔ Edit existing notes  
✔ Delete notes  
✔ Display all notes with timestamps  
✔ XSS protection using `htmlspecialchars()`  
✔ Clean file structure using a Model and simple views  
